import { Meta,Canvas  } from "@storybook/blocks";
import {Variants} from "./Test.stories.tsx";
import {Weights} from "./Weights.stories.tsx";

<Meta title="Styles/Typography" />

# Typography

Типографика — это система размеров и насыщенности шрифтов, которая используется для организации контента.


## Размеры

Вся сетка размеров представлена ниже

<Canvas of={Variants}/>

## Начертание

Есть 3 варианта начертания: `regular`, `semibold`, `bold`

<Canvas of={Weights}/>
