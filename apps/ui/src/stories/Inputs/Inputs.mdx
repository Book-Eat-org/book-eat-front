import { Meta, Canvas } from "@storybook/blocks";
import { NumberInput } from "./NumberInput.stories";
import { Input } from "./Input.stories";
import { PhoneInput } from "./PhoneInput.stories";
import { TimeInputStory } from "./TimeInput.stories";
import { Validation } from "./Validation.stories";
import {ImageInput} from "./ImageInput.stories";

<Meta title="Components/Inputs" />

# Inputs

Поля ввода - элемент, с помощью которого можно организовывать ввод с устройства

## Текстовые поля ввода

Существует 2 варианта кнопок - `primary` и `secondary`

<Canvas of={Input} />

## Числовые поля ввода

<Canvas of={NumberInput} />

## Поля ввода телефона

<Canvas of={PhoneInput} />

## Картинки

<Canvas of={ImageInput} />

## Маски

<Canvas of={NumberInput} />

<Canvas of={TimeInputStory} />

## Валидация

<Canvas of={Validation} />
