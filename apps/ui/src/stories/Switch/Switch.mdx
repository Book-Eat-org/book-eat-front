import { Meta, Canvas } from "@storybook/blocks";
import { Base } from "./Base.stories.tsx";

<Meta title="Inputs/Switch" />

# Switch

Компонент Box — это универсальный контейнер, который имеет доступ к настройкам UI системы

<Canvas of={Base} />
