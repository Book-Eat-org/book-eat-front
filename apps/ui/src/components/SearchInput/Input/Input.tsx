import styled from "@emotion/styled";

const Input = styled.input`
  border: none;
  outline: none;
  padding: 0 15px;
  flex-grow: 1;
  background: transparent;
  font-size: 16px;
`;

export default Input;